import axios from 'file://E:/VUE/mvx/node_modules/axios/index.js';

const BASE_URL = "https://elist.solz.cloud/backend/api/external/external-listings";
async function getAllListings(params = {}) {
  try {
    const response = await axios.get(`${BASE_URL}?page=1&itemsPerPage=100`, { params });
    const listings = response.data?.data || response.data?.listings || response.data || [];
    return listings;
  } catch (error) {
    console.error("Error fetching listings:", error);
    return [];
  }
}
async function getListingByRef(refNumber) {
  try {
    const response = await axios.get(`${BASE_URL}?searchTerm=elist-${refNumber}`);
    const listings = response.data?.data || [];
    console.log("Fetched from API:", listings);
    return listings.length ? listings[0] : null;
  } catch (error) {
    console.error("Error fetching property by ref:", error);
    return null;
  }
}

export { getAllListings as a, getListingByRef as g };
//# sourceMappingURL=listingsService-B5yJF8RZ.mjs.map
